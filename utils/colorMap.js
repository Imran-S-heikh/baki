const chalk = require("chalk");

const colorMap = {
    id: chalk.blue,
    action: chalk.yellow,
    amount: chalk.cyanBright,
    date: chalk.gray,
    name: chalk.greenBright
}

module.exports = colorMap;